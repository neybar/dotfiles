the fragments that are here are things that I've used for various
projects, but don't really belong in my global vimrc.  I'm now using
the localvimrc plugin.  So if you want to use the restart apache
function then grab the fragment and create an .lvimrc file in that
project's directory.
